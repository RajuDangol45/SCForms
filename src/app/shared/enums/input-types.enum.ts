export enum InputTypes {
  TEXT = 'text',
  TEXT_AREA = 'textarea',
  CHECKBOX = 'checkbox',
  RADIO_BUTTONS = 'radio',
  SELECT = 'select',
  SWITCH = 'switch',
  DATE = 'date',
  TIME = 'time',
  DATETIME = 'datetime'
}
